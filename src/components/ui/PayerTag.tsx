import { Tag } from 'antd';
import styled from 'styled-components';

export const PayerTag = styled(Tag)`
  margin-bottom: 8px;
  height: 30px;
  font-size: 16px;
  display: inline-flex;
  align-items: center;
`;
